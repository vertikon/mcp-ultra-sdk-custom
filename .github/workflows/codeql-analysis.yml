name: "CodeQL"

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run at 06:00 UTC every Monday
    - cron: '0 6 * * 1'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'go' ]
        # CodeQL supports: 'cpp', 'csharp', 'go', 'java', 'javascript', 'python', 'ruby'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
        cache: true

    # Initializes the CodeQL tools for scanning
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        # Queries: security-extended, security-and-quality
        queries: security-and-quality

        # Custom CodeQL config (optional)
        # config-file: ./.github/codeql/codeql-config.yml

    # Autobuild attempts to build any compiled languages (Go, C/C++, C#)
    - name: Autobuild
      uses: github/codeql-action/autobuild@v3

    # Alternative: Manual build steps
    # - name: Build
    #   run: |
    #     go mod download
    #     go build ./cmd

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
        upload: true
        # Output results to SARIF file
        output: results

    # Upload results as artifact (optional, for debugging)
    - name: Upload CodeQL results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: codeql-results
        path: results
        retention-days: 5
